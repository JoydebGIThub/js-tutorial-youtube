"use strict";//treat all JS code as newer version

//alert(3+3) // we use nodejs not browser

let name="Joydeb"
let age= 27
let isLoggedIn= false

console.table([typeof "Joydeb", typeof age, typeof null, typeof undefined])

/**
 * 
┌─────────┬─────────────┐
│ (index) │ Values      │
├─────────┼─────────────┤
│ 0       │ 'string'    │
│ 1       │ 'number'    │
│ 2       │ 'object'    │
│ 3       │ 'undefined' │
└─────────┴─────────────┘
 */